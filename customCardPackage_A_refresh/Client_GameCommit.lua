function Client_GameCommit (clientGame, skipCommit)
	--tbd ... considering breaking up the check and the display in Client_GameRefresh so can call the display on Game Commit regardless of whether it's time to do it or not
	--but is this required? tbd
    --checkForPendingPestilence (clientGame);
end