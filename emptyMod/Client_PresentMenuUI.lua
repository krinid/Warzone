function Client_PresentMenuUI(rootParent, setMaxSize, setScrollable, game, close)
	UI.Alert ("[PMUI] hello");
    print ("PMUI so far so good - mod menu");
    local publicGameData = Mod.PublicGameData;
end