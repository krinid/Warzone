function Client_PresentSettingsUI(rootParent)
	UI.CreateLabel (rootParent).SetText ("• When Special Units are killed, instead of dying, they become captured by the attacking player.\n• Ownership changes to the attacking player with the properties the Special Units had at the time the attack started.");
end